# ─── LLM (Claude API) ───────────────────────────────────────
ANTHROPIC_API_KEY=
# Model: e.g. claude-haiku-4-5 (see backend/config/settings.py)
LLM_MODEL=claude-haiku-4-5

# --- Speech-to-Text (OpenAI API) ---
# Required for audio transcription with speaker diarization
# Model used: gpt-4o-transcribe-diarize
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=


# ─── Speaker Diarization (pyannote) ─────────────────────────
# Create a read token at https://huggingface.co/settings/tokens
# Accept terms of use at:
# https://huggingface.co/pyannote/speaker-diarization-community-1
HUGGINGFACE_TOKEN=

# ─── Fact-Checking ──────────────────────────────────────────
# Option A: Tavily (recommended, free up to 1000 req/month)
TAVILY_API_KEY=
# Option B: SerpAPI (alternative)
# SERPAPI_API_KEY=

# ─── PostgreSQL (persistence) ───────────────────────────────
# Local dev (postgres via docker-compose on port 5433): localhost:5433
# Full stack (backend in Docker): postgres:5432
DATABASE_URL=postgresql://debategraph:debategraph@localhost:5433/debategraph

# ─── Redis (message broker) ─────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── Backend ────────────────────────────────────────────────
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8010
CORS_ORIGINS=http://localhost:5173

# ─── Whisper ────────────────────────────────────────────────
# Model: tiny | base | small | medium | large-v3 | large-v3-turbo
# Recommended for dev CPU: medium
# Recommended for GPU: large-v3-turbo
WHISPER_MODEL=medium
# Device: cuda | cpu | auto
WHISPER_DEVICE=auto
# Compute type: float16 (GPU) | int8 (CPU)
WHISPER_COMPUTE_TYPE=int8

# ─── Analysis ─────────────────────────────────────────────────
# Cosine similarity threshold for strawman detection (0.0–1.0)
STRAWMAN_SIMILARITY_THRESHOLD=0.75
# Default UI mode: judge | socratic
DEFAULT_UI_MODE=socratic
